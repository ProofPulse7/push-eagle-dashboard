/**
 * Mock merchant configuration.
 * In a real application, this data would be fetched from a database
 * or be part of the user's session/authentication context.
 */
export const merchantConfig = {
    locale: 'en-PK',
    currency: 'PKR',
};
