# Dashboard (push-eagle-dashboard Vercel project) — copy to .env.local

# Public URL of THIS Next.js app (not the Remix Shopify app)
NEXT_PUBLIC_APP_URL=https://push-eagle-dashboard.vercel.app

# Merchant/app data (public schema)
NEON_DATABASE_URL=postgresql://USER:PASSWORD@HOST/neondb?sslmode=require

# Shopify OAuth sessions (shopify_sessions schema) — same Neon cluster as push-eagle DATABASE_URL
SHOPIFY_SESSION_DATABASE_URL=postgresql://USER:PASSWORD@HOST/neondb?sslmode=require&schema=shopify_sessions

SHOPIFY_API_KEY=
SHOPIFY_API_SECRET=
SHOPIFY_DASHBOARD_SSO_SECRET=
SHOPIFY_WEBHOOK_SECRET=
SHOPIFY_SCOPES=read_customers,read_orders,read_products,write_products,write_metaobjects,write_metaobject_definitions

# Remix app URL (only used for legacy sync fallbacks)
SHOPIFY_ROOT_APP_URL=https://push-eagle.vercel.app

# Use true on development stores
SHOPIFY_BILLING_TEST=true
